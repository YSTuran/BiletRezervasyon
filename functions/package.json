{
  "name": "bilet-rezervasyon-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "sync:local-secrets": "node sync_local_secrets.js",
    "serve": "npm run sync:local-secrets && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^7.1.0",
    "pg": "^8.16.3"
  }
}
